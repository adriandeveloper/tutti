import React, { Component } from 'react';
import UserSelection from '../components/Views';

class UserSelectionScreen extends Component {
  render() {
    return (
      <UserSelection />
    );
  }
}

export default UserSelectionScreen;
